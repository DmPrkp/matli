/**
 * Авторизация временно отключена.
 *
 * Ничего не удалено: стор, страница входа, модель и роут на месте — гвард
 * в main.ts и меню в FooterBar просто пропускают проверку, пока флаг false.
 * Чтобы вернуть авторизацию, достаточно поставить true.
 */
export const AUTH_ENABLED = false;
